var DontUseUTags, Rule,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

Rule = require('./../Rule');

DontUseUTags = (function(superClass) {
  extend(DontUseUTags, superClass);

  function DontUseUTags() {
    return DontUseUTags.__super__.constructor.apply(this, arguments);
  }

  DontUseUTags.prototype.name = 'Dont Use u Tags';

  DontUseUTags.prototype.level = 'warning';

  DontUseUTags.prototype.description = "Don't use u tags\n\n```jade\n//- Invalid\nu Some text\n```";

  DontUseUTags.prototype.check = function() {
    if (this.node.type === 'Tag' && this.node.name === 'u') {
      return this.fail();
    }
  };

  return DontUseUTags;

})(Rule);

module.exports = DontUseUTags;
