var DontUseBTags, Rule,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

Rule = require('./../Rule');

DontUseBTags = (function(superClass) {
  extend(DontUseBTags, superClass);

  function DontUseBTags() {
    return DontUseBTags.__super__.constructor.apply(this, arguments);
  }

  DontUseBTags.prototype.name = 'Dont Use b Tags';

  DontUseBTags.prototype.level = 'warning';

  DontUseBTags.prototype.description = "Don't use b tags\n\n```jade\n//- Invalid\nb Some text\n```";

  DontUseBTags.prototype.check = function() {
    if (this.node.type === 'Tag' && this.node.name === 'b') {
      return this.fail();
    }
  };

  return DontUseBTags;

})(Rule);

module.exports = DontUseBTags;
