var DontLinkToFavicon, Rule,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

Rule = require('./../Rule');

DontLinkToFavicon = (function(superClass) {
  extend(DontLinkToFavicon, superClass);

  function DontLinkToFavicon() {
    return DontLinkToFavicon.__super__.constructor.apply(this, arguments);
  }

  DontLinkToFavicon.prototype.name = 'Dont link to favicon.ico';

  DontLinkToFavicon.prototype.level = 'warning';

  DontLinkToFavicon.prototype.description = "Don't like directly to `favicon.ico`\n\n```jade\n//- Valid\nlink(rel='icon' href='favicon96x96.png')\nlink(rel='icon' href='favicon96x96.ico')\n\n//- Invalid\nlink(rel='icon' href='favicon.ico')\n```";

  DontLinkToFavicon.prototype.check = function() {
    var i, len, name, ref, ref1, results, val;
    if (this.node.type === 'Tag' && this.node.name === 'link') {
      ref = this.node.attrs;
      results = [];
      for (i = 0, len = ref.length; i < len; i++) {
        ref1 = ref[i], name = ref1.name, val = ref1.val;
        if (name === 'href' && val.match(/favicon.ico/g)) {
          results.push(this.fail());
        } else {
          results.push(void 0);
        }
      }
      return results;
    }
  };

  return DontLinkToFavicon;

})(Rule);

module.exports = DontLinkToFavicon;
