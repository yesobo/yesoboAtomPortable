var DontUseSTags, Rule,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

Rule = require('./../Rule');

DontUseSTags = (function(superClass) {
  extend(DontUseSTags, superClass);

  function DontUseSTags() {
    return DontUseSTags.__super__.constructor.apply(this, arguments);
  }

  DontUseSTags.prototype.name = 'Dont Use s Tags';

  DontUseSTags.prototype.level = 'warning';

  DontUseSTags.prototype.description = "Don't use s tags\n\n```jade\n//- Invalid\ns Some text\n```";

  DontUseSTags.prototype.check = function() {
    if (this.node.type === 'Tag' && this.node.name === 's') {
      return this.fail();
    }
  };

  return DontUseSTags;

})(Rule);

module.exports = DontUseSTags;
