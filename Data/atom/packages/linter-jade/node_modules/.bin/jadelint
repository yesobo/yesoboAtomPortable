#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jadelint/bin/jadelint" "$@"
  ret=$?
else 
  node  "$basedir/../jadelint/bin/jadelint" "$@"
  ret=$?
fi
exit $ret
